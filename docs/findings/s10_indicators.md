## 10. インジ/マーケットプレイス（全❌・再テスト不要）
- トレンドフリップ族(SuperTrend/UT Bot/PMax)=long-gold/BTCベータ抽出。NWE/BB=サブコストのフェード。
- Trendlines-with-Breaks[Lux]=gold_boと+0.72冗長。自動トレンドライン(RANSAC/凸包)=賢いほど冗長。
- ❌ **SMC Fair-Value-Gap リテスト（FVGゾーンへの戻り指値）＝約定バグ修正で崩壊、死**（`research/fvg_retest_screen.py`, 2026-07-02）: 修正前は gold 15m 両サイド正（long meanR+0.27/win52%, n1.3万）＝良すぎ→バグ2件発見: (1) **mitigation判定をfill判定より先に置くと「指値に触れたが終値がゾーンを割った」最悪クラスの約定を未来の終値で消す先読み**、(2) fill足の同足ストップ無視。保守修正後（touchで約定・未約定時のみ次足キャンセル・同足ストップ計上/同足TP不計上）: **long +0.04 / short −0.03、MFE/MAE 1.02/0.99＝コスト以下の残余ベータ**。ゾーン・リテスト＝構造ブレイク再発見の法則を追認。🔬 **規約: 指値系の検証は fill判定を close系キャンセル判定より必ず先に**（§6のOHLC近似過大評価と同族の罠）。
  - **ロジック:** 因果ZigZag確定ピボットの直近X-A-B-C-Dにフィボ比(±10%)を照合→D点でリバーサル。Wボトムはネックライン超えで発火。`harmonic_winrate.py` `harmonic_trigger_search.py`
  - **生のsignal勝率＝コイン投げ**（全12パターン×gold/BTC×zz5/10で42-57%、誤差内、銘柄/長さで入替＝ノイズ）。ベースライン「任意の確定ピボットで逆張り」も≒50%。
  - **トリガー（ネックライン/確認）を足すと gold で55-57%に上がるが＝ベータ+ブレイク**：(1)同条件の "ただのロング・ブレイク(longBO)"が55%で同値＝形は無加算、(2)同パターンの空side(Mtop)は45%で50%割れ＝ロング・ベータ、(3)BTCは≒50%で転移せず。**ベータnull FAIL**。ブレイク部分は gold_bo の再発見＝冗長。
- *共通の機構:* これらは全部「トレンド継続/構造ブレイク」を別表現で再検出しているだけ＝ブックと冗長。**検出器はレバーでない。** リバーサル型(ハーモニク/ダブル)は形に方向エッジ無く、勝つ時は必ず「ロング・ベータ＋ブレイク」に化けている（空side・他銘柄・longBO同値の3点で見抜ける）。

